---
 interface Props {
  datetime: string | Date;
  className?: string;
}

const { datetime, className } = Astro.props;

const date = datetime instanceof Date ? datetime : new Date(datetime);
---

<time datetime={date.toISOString()} title={date.toLocaleString()} class={className}>
  {date.toDateString()}
</time>
